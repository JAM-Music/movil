import 'react-native';
import React from 'react';
import { render } from "@testing-library/react-native";
import Element from '_components/<%- @folder %><%- @name %>';

it('Render <%- @name %> component without crashing', () => {
  render(<Element />);
});
