/*Splash component style file*/
import {StyleSheet} from 'react-native';
import R from '_src/assets/R';
export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: R.colors.BG,
    justifyContent: 'center',
  },
});
