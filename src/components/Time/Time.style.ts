/*Time component style file*/
import {StyleSheet} from 'react-native';
import R from '_src/assets/R';
export default StyleSheet.create({
  timeStamp: {
    color: R.colors.TEXT,
    fontSize: 12,
  },
});
